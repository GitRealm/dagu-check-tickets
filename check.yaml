name: 'The name of your check here'
description: 'Process that it supports'
author: 'Your name or organization here'

branding:
  icon: 'heart'
  color: 'red'

inputs:
  token:
    description: 'the token to access remote system'
    required: true
    default: '1000'
  url:
    description: 'remote system base url'
    required: true
    default: '1000'

outputs:
  result:
    description: 'result from the execution of the script'

runs:
  using: node20
  executionpoint: dist/index.js
